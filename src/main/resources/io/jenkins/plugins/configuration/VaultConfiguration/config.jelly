<?xml version="1.0" encoding="UTF-8"?>
<?jelly escape-by-default='true' ?>
<j:jelly xmlns:j="jelly:core" xmlns:f="/lib/form" xmlns:c="/lib/credentials">
    <f:entry title="${%Vault ID}">
        <f:textbox field="vaultId" name="vaultId" />
    </f:entry>
    <f:entry title="${%Vault Secret}">
        <f:password field="vaultSecret" name="vaultSecret" />
    </f:entry>
</j:jelly>
